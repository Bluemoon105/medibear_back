server:
  port: ${PORT:8080}
  address: 0.0.0.0

spring:
  application:
    name: medibear

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://ep-withered-darkness-a1tmyqae-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_dan9lDkwSP5X

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}
      timeout: 6000       # 선택이지만 넣어두면 안정성 ↑
      ssl: false          # Railway Redis는 기본적으로 SSL off

cors:
  allowed:
    origins: https://medibear.vercel.app

fastapi:
  # 🔥 실제 FastAPI 배포 URL로 변경해야 함!
  url: ${FASTAPI_URL}

jwt:
  secret: ${JWT_SECRET}
  expiration-time: 1000000000
